<resources>

    <!-- The default font for RecyclerView items is too small.
    The margin is a simple delimiter between the words. -->
    <style name="bug_title">
        <item name="fontFamily">@font/arvo_regular</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:textColor">@color/NH_yellow</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Small</item>
    </style>

    <style name="bug_attribute_title">
        <item name="fontFamily">@font/arvo_bold</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">9sp</item>
    </style>

    <style name="bug_attribute">
        <item name="fontFamily">@font/arvo_regular</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:textColor">@color/NH_white</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="ActionBarTabStyle1" parent="@android:style/Widget.Holo.ActionBar.TabView">
        <item name="android:background">@drawable/tab_indicator</item>
    </style>

    <!-- Background repeated -->
    <style name="app_theme" parent="Theme.MaterialComponents">
        <item name="colorPrimary">@color/NH_white</item>
        <item name="colorOnSurface">@color/NH_gray</item>
        <item name="colorPrimaryDark">@color/NH_green</item>
        <item name="colorSecondary">@color/NH_red</item>
        <item name="colorOnSecondary">@color/NH_white</item>

        <item name="android:windowBackground">@drawable/app_background</item>
        <item name="android:listViewStyle">@style/TransparentListView</item>
        <item name="android:expandableListViewStyle">@style/TransparentExpandableListView</item>
    </style>

    <style name="TransparentListView" parent="@android:style/Widget.ListView">
        <item name="android:cacheColorHint">@android:color/transparent</item>
    </style>

    <style name="TransparentExpandableListView" parent="@android:style/Widget.ExpandableListView">
        <item name="android:cacheColorHint">@android:color/transparent</item>
    </style>


    <style name="Base.Widget.Design.TabLayout" parent="android:Widget">
        <item name="tabBackground">@drawable/tab_background</item>
        <item name="tabIndicatorColor">@color/NH_yellow</item>
        <item name="tabIndicatorHeight">4dp</item>
    </style>


</resources>
